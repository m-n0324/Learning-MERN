import {add ,subtract,multiply,divide} from './lecture39.js';
    let a = 10;
    let b = 20;
    let sum = add(a,b);
    let difference = subtract(a,b);
    let product = multiply(a,b);
    let divide = division(a,b);